# The package itself is the source of truth for dependencies (pyproject.toml).
# Installing it editable pulls in everything calendar-mcp needs.
-e .

# Test/development extras:
#   pip install -e ".[dev]"
